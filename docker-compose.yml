version: '3.8'

services:
  collection:
    image: buzurgmexrubon/collection
    container_name: "collection"
    restart: always
    ports:
      - "80:5000"
    networks:
      - web

networks:
  web:
    external: true
